{% extends 'default/base_admin.html.twig' %}
{% block title %}Mise a jour de l'utilisateur{% endblock %}
{%  block body %}
    {% block header %}
        {{ parent() }}
    {% endblock %}

    <h1 class="jumbotron container mt-4">Mise à jour de l'utilisateur <span class="text-info">{{ user.username }}</span></h1>
    <div class="container">
        {{ form_start(form) }}
        <div class="form-group">
            <label for="">Pseudo :</label>
            {{ form_row(form.username) }}
        </div>
        <div class="form-group">
            <label for="">Email :</label>
            {{ form_row(form.email) }}
        </div>
        <div class="form-group">
           {{ form_row(form.roles) }}
       </div>
        <button type="submit" class="btn btn-outline-warning">Modifier</button>
        {{ form_end(form) }}
    </div>
{% endblock %}